/* eslint-env mocha */
import { expect } from 'chai'
import { Random } from 'meteor/random'
import { createObjectId } from '../createObjectId'

describe(createObjectId.name, function () {
  it('is  not implemented')
})
