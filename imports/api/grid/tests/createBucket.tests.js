/* eslint-env mocha */
import { expect } from 'chai'
import { Random } from 'meteor/random'
import { createBucket } from '../createBucket'

describe(createBucket.name, function () {
  it('is  not implemented')
})
