/* eslint-env mocha */
import { expect } from 'chai'
import { Random } from 'meteor/random'
import { MediaLib } from '../MediaLib'

describe(MediaLib.name, function () {
  it('implements a get-all route')
})
